#include <stdio.h>
int main () {
    int n1=5, n2=7;
    if (n1 == n2)
        {
            printf("Numvers are Equal.");
        }
    else 
        {
            printf("Numvers are not Equal.");
        }
    return 0;
}